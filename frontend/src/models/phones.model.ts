export class Phones {
    
    name: string;
    model: string;
    price: number;
    color: string;
    

    constructor (name: string, model: string, price: number, color: string) {
        this.name = name;
        this.model = model;
        this.price = price;
        this.color = color;
    }
}