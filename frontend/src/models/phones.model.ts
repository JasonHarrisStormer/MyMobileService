export interface Phones {
    
    id: number;
    manufacterer: string;
    model: string;
    memory: string;
    price: number;
    colors: string;
    

    // constructor (name: string, model: string, price: number, color: string) {
    //     this.name = name;
    //     this.model = model;
    //     this.price = price;
    //     this.color = color;
    // }
}